'use client'

// SuiNS integration stubbed out - @mysten/suins requires Sui SDK 2.x
// which is incompatible with the current @mysten/sui@1.28.1

export function useDomain(address: string | null): string | null {
  return null
}
