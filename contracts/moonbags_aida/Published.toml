# Generated by Move + manually synced after mainnet upgrades.
# This file tracks the currently-deployed (upgraded) version of the
# moonbags_aida package. Each mainnet upgrade should bump `version` and
# `published-at` here; `original-id` and `upgrade-capability` stay
# constant across the V2 upgrade chain.

[published.mainnet]
chain-id = "35834a8a"

# Latest deployed package ID. This is what `sui client call --package`
# should target for every move call. Event queries must continue to use
# `original-id` below because Sui anchors event type signatures to it.
published-at = "0x7555b1da30fa1e0d355d18a129b2aafc92c6c9a4529e4d64ff90a2d5e692240f"

# Permanent address of the V2 publish chain. All event type strings
# (`<origin>::moonbags::CreatedEventV2` etc.) use this, regardless of
# which upgrade emitted the event.
original-id = "0x593a2e87f393dcb14e0f8c88d587c04e9bc98295e13212e8992343377bf7f313"

# Sui package version number. Bumped automatically by `sui client upgrade`.
# V1 = 2026-04-21 original publish (0x593a2e87…), V2 = 2026-04-23 upgrade
# (0x7555b1da…) that added `init_cetus_aida_pool` for Cetus auto-migration.
version = 2
toolchain-version = "1.69.2"
build-config = { flavor = "sui", edition = "2024" }

# UpgradeCap held by the admin wallet — required to call
# `sui client upgrade --upgrade-capability <cap>` on the next bump.
upgrade-capability = "0x3b1c3aca92de5b304e37de771bfb0dfc658838bdd734b4ddecae9a4e1b3df73e"
